<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appcervejaproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.ListaDetalhesCestaActivity"
            android:label="@string/title_activity_lista_detalhes_cesta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ListaTipoActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ListaMarcaActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ListaEstabelecimentoActivity"
            android:parentActivityName=".activity.ListaCestaActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.CadastroMarcaActivity" />
        <activity android:name=".activity.CadastroTipoActivity" />
        <activity android:name=".activity.CadastroEstabelecimentoActivity" />
        <activity android:name=".activity.CadastroCestaActivity" />
        <activity android:name=".activity.CadastroCervejaActivity" />
        <activity
            android:name=".activity.ListaCestaActivity"
            android:label="@string/title_lista_cesta_activity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>